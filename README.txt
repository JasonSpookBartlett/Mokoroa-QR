CLEAN GITHUB UPLOAD PACKAGE

Upload these directly into the repository root:

index.html
style.css
assets/mokoroa-logo.svg

Do not upload the containing folder itself.

The index.html includes:
style.css?v=white-theme-3

This forces phones and browsers to load the new CSS instead of the cached old version.
